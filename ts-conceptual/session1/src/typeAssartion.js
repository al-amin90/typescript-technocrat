"use strict";
{
    //
    const message = "this is the sms";
    const messageLength = message.length;
    console.log(messageLength);
    try {
    }
    catch (error) {
        console.log(error.message);
    }
    // 
}
