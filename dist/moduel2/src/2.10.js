"use strict";
{
    const arrOfNumbers = [2, 5, 5, 2];
    const arrOfString = arrOfNumbers.map((n) => n.toString());
    // const arrOfString2 : string[] = arrOfNumbers.map((num: number): string => num.toString())
    console.log(arrOfString);
    const area = {
        height: 100,
        width: "34"
    };
}
